name: EcoSync CD

on:
  push:
    branches: [ main ]

jobs:
  # ========== Job 1: 运行 CI 检查 ==========
  ci-check:
    name: CI Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build & Test
        run: mvn package -DskipTests -B

  # ========== Job 2: 构建 Docker 镜像并推送到 GitHub Container Registry ==========
  docker-build-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: ci-check
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ========== Job 3: 部署到服务器（通过 SSH） ==========
  # ⚠️ 如需启用，请先在 GitHub 仓库 Settings > Secrets 中配置以下密钥：
  #   - SERVER_HOST:     服务器 IP 或域名
  #   - SERVER_USER:     SSH 用户名
  #   - SERVER_SSH_KEY:  SSH 私钥
  #   - DB_PASSWORD:     生产环境数据库密码
  #
  # 配置完成后，取消下方注释即可启用自动部署。

  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: docker-build-push
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         script: |
  #           # 登录 GitHub Container Registry
  #           echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
  #
  #           # 拉取最新镜像
  #           docker pull ghcr.io/${{ github.repository }}:latest
  #
  #           # 停止旧容器并启动新容器
  #           docker stop ecosync-app || true
  #           docker rm ecosync-app || true
  #           docker run -d \
  #             --name ecosync-app \
  #             --restart unless-stopped \
  #             -p 8080:8080 \
  #             -e SPRING_DATASOURCE_URL=jdbc:mysql://host.docker.internal:3306/711ex?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai \
  #             -e SPRING_DATASOURCE_USERNAME=root \
  #             -e SPRING_DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }} \
  #             ghcr.io/${{ github.repository }}:latest
  #
  #           # 清理旧镜像
  #           docker image prune -f
